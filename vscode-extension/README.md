# APX Toolkit VS Code Extension

**Discover APIs and generate code directly from VS Code!**

## Features

- 🔍 **Right-click → Discover API** - Discover APIs from any URL
- 💻 **Generate Code** - Generate code snippets in 12 languages
- 📚 **Auto Documentation** - Generate API documentation automatically
- 🧪 **Test Suites** - Generate test suites in 5 frameworks
- 📦 **SDK Packages** - Generate ready-to-publish SDK packages

## Usage

1. Right-click in Explorer → "Discover API with APX"
2. Enter URL
3. Get complete API integration package!

## Installation

```bash
cd vscode-extension
npm install
npm run compile
```

Then install in VS Code:
- Press F5 to launch extension development host
- Or package and install: `vsce package`

---

**Coming soon: Full VS Code integration!**

