// This would need to run in browser console
// Let me create instructions instead
console.log("To find monetization:");
console.log("1. Open browser console (F12)");
console.log("2. Run: document.querySelectorAll('*').forEach(el => { if(el.textContent && el.textContent.toLowerCase().includes('monetiz')) console.log(el); });");
